{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "build": {
      "inputs": ["../../src/**", "build.mjs"],
      "outputs": ["dist/**"]
    },
    "test": {
      "dependsOn": ["build", "^build"]
    }
  }
}